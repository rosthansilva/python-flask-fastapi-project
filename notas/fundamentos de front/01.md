## 💻 Fase 3: Fundamentos React e Frontend

O **React** é a biblioteca JavaScript mais popular para construir interfaces de usuário. Ele permite que você construa a UI (User Interface) do seu IDP a partir de componentes isolados e reutilizáveis.

### 1\. Preparação do Ambiente Frontend

Vamos usar o **Vite** para criar o projeto React. Ele é um *bundler* moderno e muito rápido.

1.  **Mude de pasta:** Saia da pasta `idp_backend` e crie uma nova pasta para o frontend, ou crie o projeto ao lado:

    ```bash
    # Se você estava no idp_backend, volte um nível
    cd ..

    # Crie o projeto React + TypeScript usando Vite
    npm create vite@latest idp_frontend -- --template react-ts
    ```

    *Se você ainda não tem o Node.js/npm instalado, você precisará instalá-lo primeiro.*

2.  **Instale as dependências:**

    ```bash
    cd idp_frontend
    npm install
    ```

3.  **Rode a Aplicação:**

    ```bash
    npm run dev
    ```

    O frontend estará rodando em uma porta diferente, provavelmente `http://localhost:5173`.

### 2\. Conceitos Fundamentais do React

#### A. JSX (JavaScript XML)

O JSX é uma extensão de sintaxe que permite escrever código HTML dentro de arquivos JavaScript. O React o usa para descrever como a UI deve parecer.

```jsx
// Exemplo em um componente React
const saudacao = "Olá, IDP Dev!";
const Elemento = (
  <div className="card">
    <h1>{saudacao}</h1> {/* Usamos chaves {} para injetar variáveis JS */}
    <p>Construindo nossa plataforma interna.</p>
  </div>
);
```

#### B. Componentes

Um componente é uma função JavaScript que retorna JSX. É o bloco de construção básico do React.

**Exemplo: `src/components/ProjetoCard.tsx`**

```tsx
// O componente aceita um objeto 'props' (propriedades)
function ProjetoCard(props) {
  return (
    <div className="projeto-card">
      <h2>{props.nome}</h2>
      <p>Owner: {props.owner}</p>
      <small>{props.descricao}</small>
    </div>
  );
}

// Exportamos o componente para que possa ser usado em outros lugares
export default ProjetoCard;
```

#### C. Props (Propriedades)

São argumentos (dados) que você passa de um componente pai para um componente filho.

**Exemplo: `src/App.tsx` (Componente Pai)**

```tsx
import ProjetoCard from './components/ProjetoCard';

function App() {
  return (
    <div>
      <h1>Inventário de Projetos</h1>
      {/* Passando dados para o componente filho como atributos HTML */}
      <ProjetoCard 
        nome="Projeto React-FastAPI" 
        owner="Time Core" 
        descricao="Frontend da IDP" 
      />
      <ProjetoCard 
        nome="Serviço de Autenticação" 
        owner="Time Segurança" 
      />
    </div>
  );
}
export default App;
```

#### D. State (Estado) e Hooks (`useState`)

O `state` (estado) é como o React gerencia dados que **mudam** ao longo do tempo e afetam a renderização do componente. Você usa o **Hook** `useState` para criar um estado.

  * Quando o estado muda, o React **automaticamente** re-renderiza o componente.

<!-- end list -->

```tsx
import React, { useState } from 'react';

function Contador() {
  // 1. Cria o estado: 'contagem' é o valor, 'setContagem' é a função para atualizá-lo.
  const [contagem, setContagem] = useState(0); // O valor inicial é 0

  return (
    <div>
      <p>Você clicou {contagem} vezes</p>
      {/* 2. Ao clicar, chamamos a função setContagem para alterar o estado */}
      <button onClick={() => setContagem(contagem + 1)}>
        Clique para Contar
      </button>
    </div>
  );
}

export default Contador;
```

-----

## 💡 Fase 4: Tipagem com TypeScript (TS)

Este é o passo que eleva a qualidade do seu código React. O TypeScript (TS) adiciona segurança de tipos ao JavaScript, o que é crucial em projetos grandes como o seu IDP.

No React, o TS é mais importante para tipar:

1.  **Props (Propriedades)**
2.  **State (Estado)**

### Tipagem de Props

Em TypeScript, você usa **Interfaces** para definir a forma exata de um objeto, garantindo que o componente receba apenas os dados esperados.

**Exemplo: Refatorando `ProjetoCard.tsx` (Agora com TS)**

```tsx
import React from 'react';

// 1. Definir a Interface para o objeto Props
interface ProjetoProps {
    nome: string;
    owner: string;
    descricao?: string; // O '?' indica que é um campo opcional
}

// 2. Usar a Interface para tipar o componente
// Usamos React.FC<ProjetoProps> (Function Component com Props)
const ProjetoCard: React.FC<ProjetoProps> = ({ nome, owner, descricao }) => {
    // Aqui usamos a desestruturação ({ nome, owner, descricao }) para acessar as props diretamente

    return (
        <div className="projeto-card">
            <h2>{nome}</h2>
            <p>Owner: {owner}</p>
            {/* Renderização condicional: se houver descrição, mostre */}
            {descricao && <small>{descricao}</small>}
        </div>
    );
}

export default ProjetoCard;
```

**Benefício:** Se você tentar usar o `ProjetoCard` sem a prop `nome`, o TypeScript dará um erro durante o desenvolvimento, prevenindo bugs.

-----

## 🎯 Tarefa da Fase 3 & 4

1.  **Crie os Componentes:** Na pasta `idp_frontend/src/components/`, crie o componente `ProjetoCard.tsx`.
2.  **Defina a Interface:** Use a `interface ProjetoProps` para tipar o componente, garantindo que `nome` e `owner` sejam `string` e `descricao` seja uma `string` opcional.
3.  **Use o Componente:** No arquivo principal `src/App.tsx`, importe e use o `ProjetoCard`, passando os dados de projetos (como no exemplo acima).
4.  **Crie o State:** Na `src/App.tsx`, use o `useState` para armazenar o título do seu IDP e um contador simples, como no exemplo do `Contador`.

Quando terminar e sua aplicação React estiver rodando e exibindo os cards, diga **"Próximo"**. Entraremos na **Fase 5: Integração**, que conecta o seu React ao seu FastAPI.